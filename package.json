{
  "name": "ashwinphilips-com",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "PUBLIC_GIT_COMMIT_SHA=$(git rev-parse HEAD) PUBLIC_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) astro check && PUBLIC_GIT_COMMIT_SHA=$(git rev-parse HEAD) PUBLIC_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "pnpm run build && wrangler deploy",
    "deploy:preview": "pnpm run build && wrangler deploy --env preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/cloudflare": "^12.6.12",
    "@astrojs/tailwind": "^5.1.5",
    "@types/three": "^0.179.0",
    "alpinejs": "^3.14.9",
    "astro": "^5.16.15",
    "flowbite": "^3.1.2",
    "htmx.org": "^2.0.6",
    "tailwindcss": "^3.4.17",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^4.27.0"
  }
}
